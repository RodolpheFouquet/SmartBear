<%= form_with(model: bear_list, local: true) do |form| %>
  <% if bear_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bear_list.errors.count, "error") %> prohibited this bear_list from being saved:</h2>

      <ul>
        <% bear_list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
