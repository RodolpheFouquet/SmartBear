<%= form_for([@bear.bear_list, @bear]) do |f| %>
  <% if @bear.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bear.errors.count, "error") %> prohibited this bear from being saved:</h2>

      <ul>
      <% @bear.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :colour %><br />
    <%= f.text_field :colour %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
    <div class="field">
    <%= f.label :portrait %><br />
    <%= f.text_field :portrait %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
